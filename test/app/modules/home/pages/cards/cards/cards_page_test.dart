import 'package:flutter_test/flutter_test.dart';


void main() {
  testWidgets('CardsPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CardsPage(title: 'Cards')));
    //  final titleFinder = find.text('Cards');
    //  expect(titleFinder, findsOneWidget);
  });
}
