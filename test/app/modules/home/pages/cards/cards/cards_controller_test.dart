import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:hubo_test/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // CardsController cards;
  //
  setUp(() {
    //     cards = HomeModule.to.get<CardsController>();
  });

  group('CardsController Test', () {
    //   test("First Test", () {
    //     expect(cards, isInstanceOf<CardsController>());
    //   });

    //   test("Set Value", () {
    //     expect(cards.value, equals(0));
    //     cards.increment();
    //     expect(cards.value, equals(1));
    //   });
  });
}
