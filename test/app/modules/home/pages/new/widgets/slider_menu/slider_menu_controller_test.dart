import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:hubo_test/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // SliderMenuController slidermenu;
  //
  setUp(() {
    //     slidermenu = HomeModule.to.get<SliderMenuController>();
  });

  group('SliderMenuController Test', () {
    //   test("First Test", () {
    //     expect(slidermenu, isInstanceOf<SliderMenuController>());
    //   });

    //   test("Set Value", () {
    //     expect(slidermenu.value, equals(0));
    //     slidermenu.increment();
    //     expect(slidermenu.value, equals(1));
    //   });
  });
}
