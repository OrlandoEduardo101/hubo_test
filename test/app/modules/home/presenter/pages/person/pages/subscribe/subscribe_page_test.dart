import 'package:flutter_test/flutter_test.dart';
void main() {
  testWidgets('SubscribePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SubscribePage(title: 'Subscribe')));
    //  final titleFinder = find.text('Subscribe');
    //  expect(titleFinder, findsOneWidget);
  });
}
