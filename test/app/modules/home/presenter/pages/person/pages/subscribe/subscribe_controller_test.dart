import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:hubo_test/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // SubscribeController subscribe;
  //
  setUp(() {
    //     subscribe = HomeModule.to.get<SubscribeController>();
  });

  group('SubscribeController Test', () {
    //   test("First Test", () {
    //     expect(subscribe, isInstanceOf<SubscribeController>());
    //   });

    //   test("Set Value", () {
    //     expect(subscribe.value, equals(0));
    //     subscribe.increment();
    //     expect(subscribe.value, equals(1));
    //   });
  });
}
