import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:hubo_test/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // ProfileController profile;
  //
  setUp(() {
    //     profile = HomeModule.to.get<ProfileController>();
  });

  group('ProfileController Test', () {
    //   test("First Test", () {
    //     expect(profile, isInstanceOf<ProfileController>());
    //   });

    //   test("Set Value", () {
    //     expect(profile.value, equals(0));
    //     profile.increment();
    //     expect(profile.value, equals(1));
    //   });
  });
}
